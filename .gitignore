# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local config overrides
config.local.yaml

# Runtime: last discovered mount IP + persisted UI settings
.discovered_host
.ui_settings.json

# 3D-printable case designs (third-party downloads; kept local only)
[Cc]ase*/

# Data dir the Windows tool falls back to when run off-Windows (no %ProgramData%)
/OnStep/

# Editor / OS
.DS_Store
